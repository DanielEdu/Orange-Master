<div class="container">
<div class="divForms">
	<% if (user.admin==='admin') { %>
	<img src="/images/admin.png"><strong>Administrador</strong>
	<% } %>
	<% if (user.admin==='user') { %>
	<img src="/images/pawn.png"><strong>Usuario</strong>
	<% } %>
	<% if (user.admin==='trainer') { %>
	<img src="/images/trainer.png"><strong>Entrenador</strong>
	<% } %>

	<h2>Nombre: <%= user.firstName %> <%= user.lastName %></h2>
	<p>Teléfono: <%= user.phoneNumber %> </p> 
	<p>E-mail: <%= user.email %> </p>

	<a href="/user/edit/<%= user.id_user %>">Edit</a>
</div>
</div>